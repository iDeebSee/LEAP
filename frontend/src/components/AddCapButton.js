import Button from '@material-ui/core/Button';
import React from 'react';

function AddCapButton() {
    return (
        <Button variant="contained">Default</Button>
    );
}
export default AddCapButton;
